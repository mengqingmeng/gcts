spring:
  thymeleaf:
    cache: false
  http:
    encoding:
      force: true
      charset: utf-8
  output:
    ansi:
      enabled: detect
server:
  tomcat:
    uri-encoding: UTF-8
  port: 8090
logging:
  level: debug